{
  "permissions": {
    "allow": [
      "Bash(uv run:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) cd /home/neil/src/cem && uv run ruff check --fix \"$f\" && uv run ruff format \"$f\" && uv run ty check ;; esac; } || true",
            "statusMessage": "Running ruff + ty..."
          }
        ]
      }
    ]
  }
}
